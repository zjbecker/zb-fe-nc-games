export const FeaturedReview = () => {
  return (
    <p>
      featured review placeholder - this will be a single review card with the
      most votes
    </p>
  );
};

// https://nc-games-p9rv.onrender.com/api/reviews?sort_by=votes
